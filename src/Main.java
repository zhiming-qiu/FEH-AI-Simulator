import src.Field;
import src.Interactions;
import src.Unit;

class Main {
  public static void main(String[] args) {
    // test if distance-calculator.jar is loadable
    Node n = new Node(false, 0, 0);
  }
}